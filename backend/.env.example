# Real Gemini API key. Never committed — this file is only a template.
# Copy to backend/.env and fill in the real value on the target machine.
GEMINI_API_KEY=

# Port the server listens on.
PORT=4001
